[project]
name = "airdashboard"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "xunmouren", email = "xmr233168@outlook.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pygame-ce>=2.5.8",
]

[project.scripts]
airdashboard = "airdashboard:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
